let initialState = {
  friends: [
    {id:1, name: 'Alexander'},
    {id:2, name: 'Pusya'},
    {id:3, name: 'Kolya'}
  ]
}

const sideBarReducer = (state = initialState, action) => {

  return state
};

export default sideBarReducer;
